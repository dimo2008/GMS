{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "clean jsfiles",
            "type": "shell",
            "command": "Remove-Item -Recurse -Force ./dist, ./src/**/*.js, ./src/**/*.js.map",
            "problemMatcher": [],
            "group": "build"
        }
    ]
}
